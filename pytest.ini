[pytest]
asyncio_mode = auto
addopts = -ra -q --tb=short
testpaths = tests
markers =
    api: API tests (reqres.in / jsonplaceholder)
    ui: UI tests (saucedemo / the-internet)
    e2e: E2E tests (full flows)
