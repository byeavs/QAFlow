[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = "-ra -q --tb=short"
testpaths = ["tests"]
markers = [
    "api: API tests",
    "ui: UI tests",
    "e2e: E2E tests",
]

[tool.allure]
results_dir = "allure-results"
report_dir = "allure-report"
